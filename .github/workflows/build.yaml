name: build

on:
  push:
    branches: [ main ]
    paths:
      - 'JAMS2Dacier/**'
      - '.github/workflows/build.yaml'

  pull_request:
    branches: [ main ]
    paths:
      - 'JAMS2Dacier/**'
      - '.github/workflows/build.yaml'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: write
    defaults:
      run:
        shell: bash

    steps:

    #
    #  Prepare the environment
    #
    - name: Checkout Repository
      uses: actions/checkout@v6

    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x

    #
    #  Build
    #
    - name: Restore Workloads
      shell: bash
      run: dotnet workload restore

    - name: Restore dependencies
      shell: bash
      run: dotnet restore

    - name: Build
      shell: bash
      run: dotnet build --no-restore --configuration Release

    #
    #  Package
    #
    - name: Get Version
      shell: pwsh
      run: |
        $versionInfo = (Get-Item bin\Release\net10.0\JAMS2Dacier.exe).VersionInfo
        $formattedVersion = "{0}.{1}.{2}" -f $versionInfo.ProductMajorPart, $versionInfo.ProductMinorPart, $versionInfo.ProductBuildPart
        write-host "Version is: $formattedVersion"
        echo "version=$formattedVersion" | Out-File -FilePath $Env:GITHUB_ENV -Encoding utf-8 -Append

    #
    #  Push packages to NuGet.org
    #
    - name: NuGet login (OIDC → temp API key)
      uses: NuGet/login@v1
      id: login
      with:
        user: johnvotterodacier
    
    - name: NuGet push JAMS2Dacier
      shell: bash
      run: dotnet nuget push ../../NuGetPackages/Dacier.JAMS2Dacier.${{ env.version }}.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://api.nuget.org/v3/index.json

